      - name: Check generated docs are up to date
        run: |
          if ! git diff --exit-code auth/README.md; then
            echo "Generated docs are out of date. Run mise run docs and commit."
            exit 1
          fi
